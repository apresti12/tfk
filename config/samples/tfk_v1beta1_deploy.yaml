apiVersion: tfk.github.com/v1beta1
kind: Deploy
metadata:
  name: deploy-sample
  namespace: tfk
spec:
  # TODO(user): Add fields here
  serviceAccount: "test"
  terraform:
    image: "hashicorp/terraform"
    commands: ["apply"]
    version: "1.3.0"
  source:
    repo: "test"
    branch: "main"
    entryPoint: "/test"
  state:
    url: "test"
